# 프론트엔드 변경 이력 (Frontend Modification History)

## 2026-03-25 13:05:00

- **작업 내용:** 관리자 화장실 데이터 동기화 기능 추가 및 방문 횟수 UI 적용
- **상세 변경 내역:**
  - **백엔드 빌드 에러 수정 (`AdminService.java`):** `upstream` 머지 과정에서 누락된 `Inquiry` 엔티티 import 문을 추가하여 서버 기동이 가능하도록 복구했습니다.
  - **화장실 데이터 동기화 기능 (`AdminPage.tsx`):** 공공데이터 포털 API를 통해 전국 화장실 데이터를 대량(500페이지 분량)으로 가져오는 동기화 기능을 대시보드에 구현했습니다.
  - **방문 횟수 표시 UI (`ToiletPopup.tsx`):** 지도 팝업 헤더에 해당 화장실의 총 방문 횟수를 표시하는 전용 뱃지(Purple 스타일)를 추가했습니다.
  - **UI/UX 개선:** 대시보드의 불필요한 레이아웃을 정리하고, 동기화 과정에서 진행 상태(Syncing...)를 시각적으로 확인할 수 있는 로딩 처리를 적용했습니다.
- **결과/영향:** 관리자가 최신 공공데이터를 직접 동기화할 수 있는 강력한 도구가 추가되었으며, 사용자 페이지에서는 장소의 인기를 방문 횟수로 즉시 확인할 수 있게 되었습니다.

## 2026-03-25 12:40:00

- **작업 내용:** 관리자 고객지원 데이터 복구 및 지도 팝업 UI 정렬 수정
- **상세 변경 내역:**
  - **고객 지원 데이터 정상화:** 기존 Mock 데이터의 에러(Enum 불일치 등)를 해결하기 위해 데이터를 초기화하고, 실제 존재하는 유저와 연결된 30건의 정합성 있는 데이터를 재주입했습니다.
  - **데이터 생성 기능 개선 (`AdminPage.tsx`):** 관리자 페이지 내 '테스트 데이터 생성' 버튼의 API 경로를 최신화하고, 에러 발생 시 상세 내용을 콘솔에 출력하도록 디버깅 로직을 강화했습니다.
  - **팝업 UI 정렬 수정 (`ToiletPopup.tsx`):** 화장실 상세 팝업에서 후기가 없을 때 표시되는 '첫 후기 남기기' 버튼이 왼쪽으로 쏠려있던 현상을 해결하기 위해 `mx-auto` 및 `flex` 정렬을 적용하여 중앙 배치했습니다.
  - **Git 동기화:** 작업 완료 후 `upstream` 최신 코드를 병합(`pull`)하고 `origin`에 최종 푸시(`push`)를 완료했습니다.
- **결과/영향:** 관리자 페이지에서 고객지원 데이터를 정상적으로 확인 및 테스트할 수 있게 되었으며, 사용자 앱의 지도 인터랙션 UI가 보다 깔끔하게 정렬되었습니다.

## 2026-03-25 10:05:00

- **작업 내용:** 관리자 페이지 새로고침 시 잘못된 리다이렉트(404) 문제 해결
- **상세 변경 내역:**
  - **리다이렉트 경로 수정:** 세션이 없거나 권한이 부족할 때 리다이렉트하던 가상의 경로(`/404-not-found-unauthorized`)를 실제 존재하는 경로인 `/main`으로 수정했습니다.
  - **권한 체크 로직 안정화:** 전역 유저 `loading` 상태를 먼저 확인하도록 보장하고, 권한이 부족하여 튕길 때 원인 파악을 위해 유저 권한(`ROLE_...`)을 로그에 남기도록 개선했습니다.
  - **결과/영향:** 새로고침 시 세션이 복구되는 동안은 로딩 화면이 유지되며, 복구 실패 시에만 안전한 메인 페이지로 이동하게 되어 404 페이지로 이탈하는 현상을 방지했습니다.

## 2026-03-25 09:35:00

- **작업 내용:** 관리자 페이지 가시성 및 텍스트 색상 최적화
- **상세 변경 내역:**
  - **검색창 가독성 개선:** 유저 관리 탭의 검색창과 상단 통합 검색창에서 입력 텍스트가 흰색으로 나타나던 문제를 해결하기 위해 텍스트 색상을 검은색 계열(`#1A2B27`)로 명시했습니다.
  - **화장실 관리 카드 UI 수정:** 지도의 마커 클릭 시 나타나는 상세 정보 카드의 모든 흰색 텍스트(장소명, 별점, 운영 시간 등)를 어두운 색상으로 변경하여 배경과의 대비를 강화했습니다.
  - **오타 수정:** 주석 내의 '마كر' 오타를 '마커'로 수정했습니다.
- **결과/영향:** 관리자 페이지의 주요 정보와 입력 필드의 가시성이 확보되어 운영 편의성이 향상되었습니다.

## 2026-03-25 09:25:00

- **작업 내용:** EmergencySheet 컴포넌트 구문 오류 수정 및 지도 로딩 복구
- **상세 변경 내역:**
  - **구문 오류 수정 (`EmergencySheet.tsx`):** `MiniMap` 컴포넌트의 `useEffect` 내에서 시작된 `try` 블록에 `catch` 절이 누락되어 있던 문제를 해결했습니다.
  - **안정성 강화:** 지도 로딩 중 발생할 수 있는 에러를 포착하여 `mapError` 상태에 기록하고 콘솔에 출력하도록 `try-catch` 로직을 완성했습니다.
  - **기능 검증:** 수정 후 Vite 개발 서버가 정상 컴파일되었으며, `EmergencySheet` 내의 미니맵과 `/map` 페이지의 전체 지도가 모두 정상적으로 렌더링되는 것을 확인했습니다.
- **결과/영향:** 앱의 구동을 막고 있던 치명적인 문법 에러가 제거되어 서비스 이용이 가능해졌으며, 지도 기반 기능들이 모두 정상화되었습니다.

## 2026-03-25 09:16:00

- **작업 내용:** 데이터베이스(Docker) 구동 및 백엔드 서버 가동 성공
- **상세 변경 내역:**
  - **데이터베이스 가동:** `docker-compose up -d`를 통해 PostgreSQL(PostGIS) 및 Redis 컨테이너를 성공적으로 구동했습니다.
  - **백엔드 스키마 동기화:** `SPRING_JPA_HIBERNATE_DDL_AUTO=update` 옵션을 사용하여 `toilet_reviews` 테이블 등 누락된 스키마를 자동으로 생성하며 서버를 구동했습니다.
  - **가동 상태 확인:** 백엔드 서버가 정상적으로 시작되었으며, 이메일 서비스 등 초기화 로직이 수행된 것을 확인했습니다.
- **결과/영향:** 이제 프론트엔드(`http://localhost:5173`)와 백엔드(`http://localhost:8080`)가 모두 가동되어 전체 프로젝트 기능을 정상적으로 테스트할 수 있습니다.

## 2026-03-25 09:12:00

- **작업 내용:** 백엔드 및 프론트엔드 서버 가동 시도 및 계획서(`plan.md`) 작성
- **상세 변경 내역:**
  - **계획 수립:** 서버 가동 절차를 정리한 [plan.md](file:///d:/poop-map/plan.md) 파일을 생성했습니다.
  - **프론트엔드 가동:** `npm run dev` 명령어를 통해 Vite 개발 서버를 `http://localhost:5173` 포트에서 성공적으로 가동했습니다.
  - **백엔드 가동 시도:** `.\gradlew.bat bootRun`을 통해 백엔드 서버 가동을 시도했으나, 로컬 PostgreSQL 및 Redis 컨테이너(Docker)가 구동되지 않아 데이터베이스 연결 오류로 인해 중단되었습니다.
- **결과/영향:** 프론트엔드 UI 확인은 가능하나, 로그인 및 데이터 조회 등 백엔드 연동 기능은 Docker Desktop 실행 후 데이터베이스 서비스가 활성화되어야 정상 작동합니다.

## 2026-03-24 18:03:00

- **작업 내용:** Swiper 라이브러리 설치
- **상세 변경 내역:**
  - 캐러셀/슬라이더 UI 구현을 위해 `swiper` 패키지를 프론트엔드 프로젝트(`package.json`)에 추가로 설치했습니다.
- **결과/영향:** 향후 이미지 갤러리 뷰어나 카드 슬라이드 형태의 컴포넌트를 쉽고 안정적으로 구현할 수 있는 기반이 마련되었습니다.

## 2026-03-24 17:55:00

- **작업 내용:** 리뷰 리스트 조회 시 데이터 렌더링 누락 문제 해결 (API 스펙 동기화)
- **상세 변경 내역:**
  - **프론트-백엔드 데이터 모델 불일치 해결 (`reviewService.ts`, `ReviewListModal.tsx`):**
    - 프론트엔드 코드에서는 백엔드의 페이지네이션 응답 객체 내부의 리스트 필드를 `content`로 기대하고 있었으나, 실제 백엔드 커스텀 DTO(`ToiletReviewPageResponse`)는 `contents`로 결과를 반환하고 있었습니다. 이로 인해 리스트 목록이 빈 화면으로 나오는 현상과 타입 에러가 발생했습니다.
    - `ToiletReviewPageResponse` 인터페이스 사양을 `contents`, `currentPage`, `hasNext` 등 백엔드 응답 형태와 100% 일치하도록 수정하고, 렌더링 참조(`reviewData.content` -> `reviewData.contents`)를 변경하여 리스트가 정상적으로 화면에 그려지도록 개선했습니다.
- **결과/영향:** 프론트엔드의 데이터 바인딩 오류를 제거하여 "전체 후기 보기" 시 실제 후기 목록이 정상적으로 출력되며 기능의 완성도를 높였습니다.

## 2026-03-24 17:52:00

- **작업 내용:** 리뷰 목록 렌더링 예외 처리 강화 및 안내 문구 서식 수정
- **상세 변경 내역:**
  - **리뷰 목록 안정성 보강 (`ReviewListModal.tsx`):**
    - 백엔드 응답의 `content` 배열이 `null`이거나 `totalPages`가 예상치 못한 타입일 경우 발생하던 렌더링 오류(ErrorBoundary 트리거)를 방지하기 위해 옵셔널 체이닝 및 타입 방어 코드를 추가 적용했습니다.
    - 평균 별점(`avgRating`) 계산 시 발생할 수 있는 모든 `null` 참조 포인트를 `?? 0` 로직으로 완벽히 차단했습니다.
  - **사용자 안내 메시지 서식 수정 (`ToiletPopup.tsx`):**
    - 방문 인증 실패 알림창의 텍스트와 이모지 구성을 사용자의 요청에 맞게 미세 조정(줄바꿈 및 공백 반영)했습니다.
- **결과/영향:** 오류 페이지 노출 빈도를 줄여 서비스 신뢰도를 높이고, 사용자 요청을 세밀하게 반영하여 만족도를 개선했습니다.

## 2026-03-24 17:48:00

- **작업 내용:** 리뷰 목록 조회 오류 수정 및 후기 작성 권한(방문 인증) 강화
- **상세 변경 내역:**
  - **리뷰 목록 안정성 확보 (`ReviewListModal.tsx`):** 평균 별점(`avgRating`) 데이터가 없을 때 `toFixed`를 호출하여 발생하는 런타임 오류(ErrorBoundary 행 현상)를 방지하기 위해 널 병합 연산자(`?? 0`) 및 옵셔널 체이닝을 적용했습니다.
  - **후기 작성 권한 강화 (`ToiletPopup.tsx`):** '후기 남기기' 클릭 시 기존 로그인 체크 외에 **방문 인증 여부(`toilet.isVisited`)**를 추가로 확인하도록 변경했습니다. 방문하지 않은 사용자에게는 후기 작성을 제한하고 안내 메시지를 표시합니다.
  - **데이터 연동 알림:** 현재 화장실 목록 API는 개별 방문 상태를 직접 내려주지 않으므로, 추후 백엔드에서 `isVisited` 필드를 연동하면 보다 정교한 권한 제어가 가능함을 명시했습니다.
- **결과/영향:** 서비스 안정성을 높이고, 실제 방문자 중심의 신뢰도 높은 리뷰 생성을 장려하는 시스템 정책을 구현했습니다.

## 2026-03-24 17:45:00

- **작업 내용:** 모달 레이아웃 최종 최적화 및 리뷰 시스템 권한/동기화 로직 보완
- **상세 변경 내역:**
  - **레이아웃 정중앙 배치 (`ToiletPopup.tsx`, `MapPage.tsx`):**
    - 정보 모달이 상단으로 쏠려 잘리는 현상을 해결하기 위해, 내부의 마커 기준 상대 좌표 스타일(`bottom: 110%`)을 제거하고 `MapPage`의 중앙 정렬 컨테이너(`top: 50%`)에 완전히 맞추도록 수정했습니다.
  - **리뷰 작성 권한 체크:** 비로그인 사용자가 후기 작성 버튼 클릭 시 로그인 모달이 뜨도록 `handleOpenReviewModal` 로직을 추가했습니다.
  - **후기 데이터 실시간 동기화 해결:** 후기 작성 후 카드 내의 후기 개수와 목록이 즉시 갱신되지 않던 문제를 해결하기 위해, 성공 콜백 시 `onReviewUpdate`를 통해 부모(MapPage)의 전체 데이터를 `refetch`하도록 통합했습니다.
- **결과/영향:** 정보 가시성 문제를 완벽히 해결하고, 인증 기반의 안전한 리뷰 작성 및 데이터 일관성을 확보했습니다.

## 2026-03-24 17:42:00

- **작업 내용:** 화장실 정보 모달 위치 조정 및 리뷰 모달 렌더링 버그 수정
- **상세 변경 내역:**
  - **모달 위치 조정 (`MapPage.tsx`):** `selectedToilet` 정보창의 수직 위치를 `75%`에서 `50%`로 수정하여 화면 중앙에 오도록 레이아웃을 개선했습니다.
  - **리뷰 모달 버그 해결 (`ToiletPopup.tsx`):**
    - 부모 요소의 `transform` 속성으로 인해 `fixed` 포지션인 리뷰 모달이 팝업 내부에 갇히거나 위치가 어긋나는 현상을 발견했습니다.
    - `react-dom`의 `createPortal`을 사용하여 리뷰 작성 및 목록 모달이 `document.body` 레벨에서 렌더링되도록 구조를 변경하여 위치 계산 문제를 근본적으로 해결했습니다.
- **결과/영향:** 정보 피카뷰의 가독성을 높이고, 리뷰 작성 기능을 모든 상황에서 정상적으로 사용할 수 있도록 시스템 안정성을 확보했습니다.

## 2026-03-24 17:40:00

- **작업 내용:** 지도 마커 모달(`ToiletPopup`) 버튼 인터랙션 강화 및 트렌디한 UI 적용
- **상세 변경 내역:**
  - **Framer Motion 적용:** 모든 버튼 태그를 `motion.button`으로 교체하고 `whileHover`, `whileTap` 속성을 통해 즉각적인 시각적 피드백을 제공합니다.
  - **애니메이션 디테일:**
    - **즐겨찾기 및 닫기:** 호버 시 회전(rotate) 및 스케일 업 효과를 추가하여 역동성을 높였습니다.
    - **길찾기 버튼:** 카카오/네이버 버튼에 미세한 기울기 변화와 그림자 효과를 적용했습니다.
    - **방문 인증 버튼:** 인증 가능 상태일 때 버튼 위를 지나가는 **Shimmer(빛 번짐) 애니메이션**을 추가하고, 호버 시 브라이트니스 및 그림자 강도를 높여 클릭하고 싶은 디자인을 구현했습니다.
  - **리뷰 버튼:** 배경색 부드러운 전환(transition-colors)과 스케일 조정을 통해 클릭 경험을 개선했습니다.
- **결과/영향:** 지도 기반의 핵심 인터랙션 요소들에 세밀한 애니메이션을 적용하여 서비스의 전반적인 완성도와 '리액트스러운' 최신 앱 느낌을 강화했습니다.

## 2026-03-24 17:35:00

- **작업 내용:** 원격 저장소 동기화 및 화장실 리뷰 기능 통합
- **상세 변경 내역:**
  - **Git 동기화:** `upstream/main`으로부터 최신 변경 사항을 풀(pull)하고, `origin/main`으로 푸시(push)하여 팀원과의 코드 베이스를 동기화했습니다.
  - **충돌 해결 (`V3__add_toilet_reviews.sql`):** 데이터베이스 마이그레이션 파일의 PostgreSQL 호환성(`IF NOT EXISTS`) 구문 충돌을 상위 브랜치 기준으로 해결했습니다.
  - **리뷰 기능 통합:**
    - `ReviewModal.tsx`, `ReviewListModal.tsx`: 화장실 리뷰 작성 및 목록 조회 UI 컴포넌트를 추가했습니다.
    - `reviewService.ts`: 리뷰 API 통신 로직을 통합했습니다.
  - **서버 관리:** 작업 진행을 위해 구동 중이던 백엔드(8080) 및 프론트엔드(5173) 서버를 정상 종료했습니다.
  - **서버 재가동:** 동기화 완료 후 백엔드와 프론트엔드 서버를 재가동했습니다. 백엔드 구동 시 발생한 `toilet_reviews` 누락 이슈를 `ddl-auto: update`를 통해 해결하고 다시 `validate`로 전환했습니다.
- **결과/영향:** 최신 협업 코드를 반영하고 새롭게 구현된 리뷰 시스템을 안정적으로 통합하여 전체 서비스를 정상 가동 상태로 전환했습니다.

---
[✅ 규칙을 잘 수행했습니다.]
